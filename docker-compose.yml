services:
  web:
    build:
      context: .
      dockerfile: compiler/Dockerfile
    image: tex2pdf-compiler
    command: ["true"]

# This compose file only builds the compiler image.
# The FastAPI app runs directly on your host during MVP development.
